<UserControl x:Class="RimWorldModBrowser.Components.ModList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
             xmlns:local="clr-namespace:RimWorldModBrowser.Components"
             mc:Ignorable="d"
             Loaded="OnLoad"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid x:Name="LayoutRoot">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <xctk:WatermarkTextBox x:Name="tb_searchBox" VerticalAlignment="Center" Style="{Binding Path=TextBoxStyle}" TextChanged="SearchBox_TextChanged" Watermark="{Binding Path=GhostText}" />
        <ListBox Grid.Row="1" ItemsSource="{Binding Path=FilteredModList}" SelectionChanged="ListBox_SelectionChanged" Style="{Binding Path=ListBoxStyle}"/>
    </Grid>
</UserControl>
